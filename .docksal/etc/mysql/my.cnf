[mysqld]

# Enable slow query logging
#long_query_time=1
#slow_query_log=1
#slow_query_log_file=/dev/stdout

# Disable binary logging
skip-log-bin

# Disable performance schema to drastically reduce memory usage
performance_schema = 0
